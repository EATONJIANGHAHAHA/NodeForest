export default class {
    constructor (
        adminId,
        username,
        password,
    ) {
        this.adminId = adminId;
        this.username = username;
        this.password = password;
    }
}